<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!-- Authentication tokens, device identifiers and API credentials are
         stored in SharedPreferences and must never be copied to backups. -->
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
